<style>
    .profile-picture {
        width: 150px;
        /* Set the width and height to create a circular shape */
        height: 150px;
        /* Background color for the empty box */
        border-radius: 50%;
        /* Create a circular shape */
        margin: 0 auto 20px;
        /* Center horizontally and add some margin at the bottom */
        /* Add a border around the circle */
        overflow: hidden;
        /* Hide content that might overflow */
        display: flex;
        justify-content: center;
        align-items: center;
    }

    /* Style the circular profile picture's border */
    .profile-picture img {
        width: 100%;
        height: auto;
        display: block;
    }

    .imageScaler {
        width: 10%;
        height: auto;
        /* Allow the height to adjust proportionally based on the width */
        max-width: 100px;
        /* Set a maximum width if necessary */
    }

    /* Reset default margin and padding for body and form elements */
    body,
    form {
        margin: 0;
        padding: 0;
    }

    /* Create a flex container to center the form vertically and horizontally */
    .container {
        display: flex;
        justify-content: center;
        /* Horizontally center the form */
        align-items: center;
        /* Vertically center the form */
        min-height: 100vh;
        /* Make the container at least viewport height */
        /* Optional background color */
    }

    /* Style the form */
    .centered-form {
        text-align: center;
        /* Center-align form contents */
        padding: 20px;
        /* Add some padding around the form */
        /* Form background color */
        /* Add a subtle shadow */
    }

    /* Style form elements */
    .centered-form .form-control {
        width: 100%;
        /* Input width is 100% of the form container */
        margin-bottom: 15px;
        /* Add space below the input */
        border-radius: 5px;
        /* Add rounded corners to the input */
    }

    .centered-form button {
        padding: 10px 20px;
        /* Add padding to the button */
        border: none;
        /* Remove button border */
        cursor: pointer;
        /* Add a pointer cursor on hover */
        height: 45px;
        /* Set the desired height */
        width: 100%;
    }

    .centered-form button:hover {
        background-color: rgba(255, 255, 255, 0.5);
        /* Change button background color on hover */
    }

    .transparent-bg {
        background-color: rgba(255, 255, 255, 0.5);
        /* Adjust the alpha value (0.5 in this example) for the desired level of transparency */
    }

    /* Remove the default border and focus outline for form inputs */
    .form-control {
        border: none;
        border-radius: 0;
        /* Optionally, remove any border-radius */
        border-bottom: 1px solid #000;
        /* Add a border only to the bottom */
        padding: 5px 0;
        /* Add some padding to create space between the input and the underline */
        background-color: transparent;
        /* Make the background transparent */
        transition: background-color 0.3s;
        /* Add a smooth transition for background color */
        outline: none;
        /* Remove the default focus outline */
    }

    /* Style the input when it's focused (optional) */
    .form-control:focus,
    .form-control:active {
        border-bottom: 2px solid #A92036;
        /* Change the underline color or style on focus */
        background-color: transparent;
        /* Make the background transparent on focus or when active */
    }

    /* Style the form */
    .form-group {
        margin-bottom: 20px;
        /* Add some margin between form groups (optional) */
    }

    /* Style form labels (optional) */
    .form-label {
        color: #333;
        /* Change label text color */
    }
</style>
<a title="Logout" href="/logout" class="floating-button-logout "><img src="/images/icons/power.svg"
        class="logoutImage"></a>

<style>
    .floating-button-logout {
        width: 50px;
        height: 50px;
        padding: 10px 16px;
        font-size: 24px;
        line-height: 1.33;
        position: fixed;
        bottom: 10px;
        right: 10px;
        z-index: 3;
        border-radius: 50%;
        /* Use 50% for a circular shape */
        border: 1px solid black;
        /* Add a black border */
        background-color: transparent;
        /* Make the background transparent */
        display: flex;
        justify-content: center;
        align-items: center;
        /* Center the content vertically */
        text-decoration: none;
        /* Remove underline */
        color: black;
        /* Text color */

    }

    .floating-button-logout:hover {
        transform: scale(1.2);
    }

    .logoutImage {
        width: 100%;
        height: 100%;
        object-fit: scale-down;
    }
</style>